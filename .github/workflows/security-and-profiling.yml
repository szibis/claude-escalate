name: Security & Performance Verification

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  security-events: write

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: golangci-lint with gosec
        run: go run github.com/golangci/golangci-lint/cmd/golangci-lint@latest run ./... --enable gosec --timeout 10m

      - name: Build
        run: make build

  memory-and-profiling:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Memory Leak Detection
        run: go test -v -run TestMemoryLeak ./... -timeout 30s 2>&1 || true

      - name: SLO Tests
        run: go test -v -run "TestSLO" ./... -timeout 30s 2>&1 || true

      - name: Profiling Tests
        run: go test -v -run "TestCPU|TestHeap|TestGoroutine|TestAllocation|TestLatency" ./... -timeout 60s 2>&1 || true

      - name: Generate CPU profile
        run: |
          mkdir -p profiles
          go test -bench=. -cpuprofile=profiles/cpu.prof ./... 2>&1 | tee profiles/bench.txt || true

      - name: Generate Memory profile
        run: go test -bench=. -memprofile=profiles/mem.prof ./... || true

      - name: Security Test Suite
        run: go test -v ./internal/security/... -timeout 30s

      - name: Upload profiles
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: profiling-results
          path: profiles/
          retention-days: 30

  fuzzing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Fuzz Testing
        run: |
          go test -fuzz=Fuzz ./... -fuzztime=10s -timeout=30s 2>&1 || true

  stress-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Stress Tests with Race Detection
        run: go test -v -race -short -timeout 120s ./... 2>&1 || true
