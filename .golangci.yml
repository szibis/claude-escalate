version: "2"

run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - bodyclose
    - copyloopvar
    - gofmt
    - goimports
    - goprintffuncname
    - gosec
    - misspell
    - nolintlint
    - unparam
    - whitespace
  settings:
    misspell:
      locale: US
    staticcheck:
      checks: ["all"]
    gosec:
      excludes:
        - G104
        - G304

issues:
  max-issues-per-linter: 50
  max-same-issues: 10
