version: "2"

run:
  timeout: 5m

formatters:
  enable:
    - gofmt
    - goimports

linters:
  enable:
    - govet
    - ineffassign
    - staticcheck
    - bodyclose
    - copyloopvar
    - goprintffuncname
    - gosec
    - misspell
    - nolintlint
    - unparam
    - whitespace
  disable:
    - errcheck  # phase-1-7 sentiment-budgeting new code needs error handling refactor
    - unused
  settings:
    misspell:
      locale: US
    gosec:
      excludes:
        - G104
        - G304

issues:
  max-issues-per-linter: 50
  max-same-issues: 10
